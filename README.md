# mycreds-frontend

React and Vite frontend for MyCreds.

## Local setup

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_BASE_URL` to your backend URL for deployed environments.
